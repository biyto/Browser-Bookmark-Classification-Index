# Popup 交互细节

> 需要了解当前项目进度时，先读取 `PROJECT_STATUS.md`。

## 布局

popup 采用宽菜单式布局，建议宽度为 200-680px。

```text
顶部：搜索框、主题按钮、同步按钮、后台设置入口
路径：完整分类面包屑（高度数值尽量小，不影响主体视觉体验）
主体：最多三列级联分类内容
底部：固定最近常用入口
```

## 顶部操作

- 同步按钮用于触发浏览器书签读取，并保存本地书签快照。
- popup 默认不自动同步书签，除非后台设置将同步方式切换为“打开 popup 时同步”。
- popup 不负责分类匹配规则，不在前端代码中内置关键词、域名或收藏夹路径匹配。
- 同步后如果书签已有本地分类关联，则保留原关联。
- 同步后如果书签没有本地分类关联，则默认进入“未分类”。
- 分类匹配规则、规则预览和批量应用后续放在后台控制页。

## 三列级联

popup 同时最多展示三列。

交互：

- 设置三级宽度，初始保持最低一级宽度。
- popup 靠近浏览器右侧时，一级分类列保持在右侧，二级、三级分类向左展开。
- 检测悬停一级分类时间，达到设置时间后，页面使用流畅动画效果变成二级宽度，在左侧展示二级内容。
- 检测悬停二级分类时间，达到设置时间后，页面使用流畅动画效果变成三级宽度，在更左侧展示三级内容。
- 悬停展开下一级时，当前鼠标所在层级不允许缩窄、位移或改变鼠标指向；只新增下一级内容。
- 只有鼠标移动进入下一级后，上一级才允许缩窄。
- 鼠标光标从一级分类移动到二级分类后，一级分类使用流畅动画效果向右缩窄，popup保持二级宽度不变，只是二级分类宽度占比更大。
- 鼠标光标从二级分类移动到三级分类后，二级分类使用流畅动画效果向右缩窄，一级分类再次缩窄，popup保持三级宽度不变，只是三级分类宽度占比更大，二级分类宽度变为上一步中一级分类宽度，一级分类最窄。
- 继续悬停更深层级时，最右侧层级向右侧缩窄至隐藏，右侧第二层级变为最右侧层级宽度，后续层级依次变化，总宽度保持三级不再变化。
- 顶部面包屑始终显示完整路径。
- 面包屑视觉方向需与向左展开一致：根分类靠右，当前分类向左延展。
- 点击面包屑任意节点，回到对应上级。
- 悬停展开延迟、popup 宽度动画速率、列宽变化动画速率需预留后台设置项。

示例：

```text
音乐 / 流行乐 / 华语流行
```

如果继续进入更深层级，界面仍只保留最近三列。

## 分类列内容

每一列都按同样规则展示：

1. 直接子分类。
2. 当前分类范围内的书签。

当前分类范围包括该分类自身及所有下级分类。

示例：

```text
音乐
  流行乐
    华语流行
      网站 A
    欧美流行
      网站 B
  古典
    网站 C
```

悬停“音乐”时：

```text
子分类：流行乐、古典
书签：网站 A、网站 B、网站 C
```

悬停“流行乐”时：

```text
子分类：华语流行、欧美流行
书签：网站 A、网站 B
```

## 最近常用

“最近常用”位于普通一级分类底部，并保持固定。

行为：

- 悬停后展开最近高频网页。
- 不显示子分类。
- 不允许添加子分类。
- 普通一级分类滚动时，它不消失。

## 主题颜色

- popup 顶部提供主题颜色小按钮。
- 当前版本允许快速切换预设主题色。
- 后续后台设置需提供主题色配置入口，并复用 popup 当前本地设置字段。

## 书签项展示

书签项应高密度、易扫描：

- 单行或紧凑双行。
- 标题优先。
- URL 或域名作为弱信息。
- 避免大卡片。
- 点击后打开对应书签。
- 第一版使用浏览器 favicon 能力展示网站小图标，不把 URL 发送给外部缩略图服务。
- 后续用户自行添加书签时，需在不破坏隐私原则的前提下完善网站缩略图能力。
