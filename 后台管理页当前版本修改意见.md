# 后台管理页当前版本修改意见

> 本文件用于记录后台管理页第一版的 UI、文案、交互和功能问题。
> 后续修改后台管理页前，先读取 `PROJECT_STATUS.md`，再读取 `BACKEND_CONTROL.md` 和本文档。

## 当前基线

- 后台入口：`src/options/options.html`。
- 后台逻辑：`src/options/options.js`、`src/background/background.js`。
- 共享存储：`src/shared/storage.js`。
- 本版只做后台控制页第一版、后台定时同步、AI 配置骨架和规则中心占位。

# 记录问题

## 基础设置
 - popup 、favicon等英文要改掉，说了目前版本是面向中文用户。实在没法翻译的，也要解释这个有什么用。
 - 主题色修改后，popup状态好像没有同步、实际主题色也没有修改。并且我最开始想要可以切换主题色，是为了有个暗夜模式。
 - 关于右侧悬停和展开的设置，已作为弃案不用了，设置里删掉不要有。
 - 自定义强调色好像做成了和主题色一样的功能，没什么用。

## 同步管理
 - 感觉这一部分可以直接并入基础设置。

## 分类管理 
 - 分类树每个要分类默认关闭，点击再展开子一级分类
 - 分类树页面要有增减分类、重命名分类等功能，要能直接拖动改变分类顺序，并且需要同步到popup
 - 在右上角加一个是否显示分类下书签页开关。开启后，页面变为左右两半，左半依旧是分类树，右半是当前所选分类级别下的所有书签。同样可以增改、重命名书签，快捷拖动书签更改其所属分类。更改后需要同步记录所有的更改状态，为防止误操作，用户点击确认后更改才生效。
 - 要把所有的分组名进行记录，并用一个数据进行储存，后续使用AI分类时，AI需优先在已有分类中进行匹配，匹配失败再决定是否创建新的一级/二级/三级等分类等级 
 - AI自动分类只分析未分类书签，不要分析已分类书签，除非用户勾选全部重新分类。分类是否应用需由用户决定

## 书签归类
 - 该功能可能会在分类管理功能完善后与其重复，具体如何修改或是删除需依据后续项目进展。

## 规则中心
 - 分类规则协议已升级为规则组：支持归类规则组、移出分类规则组、规则新增/编辑/删除、预览勾选和应用。
 - 当前规则支持多匹配字段、单一匹配方式、多目标分类；目标分类组通过真实分类层级筛选后勾选，仅显示实际存在的层级，并支持三级及更深层级。
 - 归类规则组只用于默认/无分类书签的初次匹配；移出分类规则组用于把已分类书签从指定分类范围移出。应用后只更新插件本地分类关联，不修改浏览器原始书签，不强制归入末端分组。
 - 新增规则组整个面板默认收起；匹配字段和匹配方式下方已补充小字说明。
 - AI 自动分类未处理书签需要作为独立功能入口，不应混在新增规则表单里；当前仅保留预留按钮。
 - 重要风险：规则中心后续应继续保持“先预览、可勾选、再应用”的轻量任务流，避免重新堆叠复杂配置项。
