# 项目当前进度

更新时间：2026-06-06

## 当前状态

- Git 已初始化，当前主线分支为 `main`。
- 第一版 popup 已完成并合并到 `main`。
- 当前功能基线提交：`76dd5ba 实现后台控制页第一版`。
- 后台管理开发说明已拆分到 `BACKEND_CONTROL.md`，后续开发后台时优先读取该文档。

## 已完成

- Manifest V3 扩展基础结构，可在 Chrome / Edge 加载。
- popup 三列级联分类启动器。
- 分类向左展开，一级分类保持在右侧。
- 悬停展开下一级时，当前鼠标所在列不缩窄、不位移。
- 面包屑方向已修复：根分类靠右，当前分类向左延展。
- 本地分类树、书签多分类关联、向下汇总查询。
- 本地搜索，匹配标题、URL、域名和分类。
- 最近常用入口与插件内点击统计。
- popup 顶部主题色按钮和主题设置存储。
- 书签 favicon 展示。
- popup 顶部手动同步按钮、本地书签快照和同步元信息。
- popup 前端已移除书签分类匹配规则；同步时只保留已有本地分类关联，新书签默认进入“未分类”。
- 后台控制页第一版：基础设置、同步管理、分类管理、书签归类、规则中心、AI 配置、关于。
- 分类管理页已进入增强阶段：支持分类树新增、重命名、删除、拖拽排序、展开层级控制，以及右侧分类书签栏和批量管理开关。
- 分类管理右侧书签栏已接入本地 favicon 数据，并修复书签复制/移动到目标分类的本地关联更新。
- 分类管理右侧书签栏已增加统一拖拽模式切换、悬停三点菜单、悬停直接移除，以及单个/批量书签移动、复制、移除。
- 分类管理左侧分类树已接入批量管理初版，勾选多个分类后可作为一组拖动调整位置。
- 分类管理已新增分类数据导入/导出，导入支持覆盖、保留原有分类和仅新增无冲突数据三种策略；卸载前清理入口可先导出分类数据，再清空插件本地 storage/cache。
- 规则中心已重构为规则组：包含归类规则组和移出分类规则组、规则新增/编辑/删除、字段多选、匹配方式单选、真实分类层级选择、预览勾选和应用。
- 归类规则组只处理默认/无分类书签；移出分类规则组可把已分类书签从指定分类范围移出，应用后只更新本地分类关联，不修改浏览器原始书签。
- 规则中心新增规则组面板默认收起；“AI 处理未分类书签”入口已移动到分类管理页，并已接入统一 AI 请求分发。
- popup 设置按钮已接入后台控制页。
- MV3 `background.service_worker` 已接入 `chrome.alarms`，支持后台定时同步。
- AI 配置已支持 OpenAI 兼容格式厂家配置、可选 Chat Completions / Responses 接口、JSON Schema / JSON Object / Text 响应格式、temperature、max tokens、超时、流式开关、自定义 Header / Body，以及按厂家保存的两套任务提示词。
- AI 配置页“新增厂家”和“已配置厂家”默认折叠；已配置厂家列表按三列显示摘要，点击单个厂家展开详情，展开一个厂家时自动收拢其它厂家。
- AI 配置页支持测试新建厂家、单个已配置厂家，以及直接测试当前启用 API 配置；当前启用厂家有明确状态提示和“设为当前启用”操作。
- 书签 AI 自动分类已改为单轮分批分析流程：每批同时生成分类体系草案和临时归类缓存，并持续携带已生效分组、候选分组和分组整理记录；用户确认体系后，本地按候选路径映射修正缓存归类结果并生成预览，不再第二次发送书签给 AI。
- 书签归类继续保持多标签模型，一个书签可同时写入多个本地分类；应用过程只更新 `bookmarkCategoryLinks`，不修改浏览器原始书签数据。
- AI 默认分类提示词已强化多标签判断：要求模型主动检查内容主题、使用目的、工具属性、平台/资源类型等维度，命中多个独立维度时返回 2-3 个分类路径。
- AI 自动分类已调整优先级：优先把书签归入多个已生效分组或候选分组，只有多标签组合仍无法覆盖共同用途时才建议新增分组。
- AI 提示词当前按任务分为 `bookmarkClassification` 和 `bookmarkIndexing` 两套，分别用于书签自动分类和后续书签索引/模糊搜索，不应混用。
- 分类管理页的 AI 入口已改为任务卡样式，展示当前 AI 厂家、每批数量、发送字段、本地分组状态、待处理未分类书签数量和响应流程；请求超时时会自动拆小当前批次重试，长任务期间会显示阶段和批次进度状态，且本地只接受当前任务临时编号映射中的书签标记。
- 新增 `AI自动分类提示词与接口约定.md`，记录书签自动分类提示词、任务键、请求结构和响应结构。
- 后台管理相关文档已从 popup 交互文档中拆出，减少后续后台开发被 popup 细节干扰。
- 后台 options 主脚本已完成模块拆分：`options.js` 仅保留启动编排；公共状态和工具在 `options-common.js`；分类管理在 `options-categories.js`；规则中心在 `options-rules.js`；AI 配置与 AI 分类任务在 `options-ai.js`；事件绑定在 `options-events.js`；跨模块刷新注册在 `options-render.js`。

## 权限与隐私

- 当前权限：`alarms`、`bookmarks`、`storage`、`favicon`。
- 已增加可选主机权限 `optional_host_permissions`，仅在用户测试或执行 AI 请求时按目标接口域名请求授权。
- 不申请 `history`。
- 不把书签、网页正文或摘要发送给外部 API。
- 不硬编码 API Key 或密钥。
- popup 不抓取网页正文，不在前端执行关键词、域名或收藏夹路径分类匹配。

## 已预留

- `popupSettings`：悬停延迟、popup 宽度动画、列宽动画、展开方向。
- `appearanceSettings`：主题色、是否显示 favicon。
- `bookmarkSyncSettings`：同步方式、后台控制器、后台同步间隔。
- `bookmarkSyncMeta`：最近同步时间、同步数量、同步来源、同步方式、同步状态和最近错误。
- `languageSettings`：语言接口，当前仅保留 `zh-CN`。
- `aiSettings`：AI 厂家列表、当前启用厂家、接口 URL、API Key、模型、OpenAI 兼容请求参数、按厂家区分的任务提示词、默认分析字段和 AI 任务参数；分类默认每批 10 条，默认超时 90 秒。
- `classificationRules`：分类匹配规则组容器，当前版本为 `version: 3`，包含一条可用示例归类规则组，并兼容旧版规则迁移。

## 下一步

- 根据 `后台管理页当前版本修改意见.md` 记录的问题继续调整后台页面。
- 在 Chrome / Edge 中重新加载扩展，实测 options page、service worker 和 alarms。
- options page 后续新增后台能力时，按当前模块边界维护，避免重新堆回单一大文件。
- 规则中心当前已改为规则组和预览确认流；后续需要在 Chrome / Edge 中实测大量书签下的预览性能、逐条勾选和应用流程。
- 后续完善分类管理的书签展示排版、操作图标、批量管理、备份恢复和缩略图能力。
- 后续完善 AI 自动分类结果预览、用户逐条确认和应用写入流程。
