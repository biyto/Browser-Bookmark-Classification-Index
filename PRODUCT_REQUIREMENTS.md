# 产品需求

> 需要了解当前项目进度时，先读取 `PROJECT_STATUS.md`。

## 产品定位

本项目是一个同时适用于 Microsoft Edge 和 Google Chrome 的浏览器插件，当前主要面向中文用户。

插件基于浏览器原生书签建立本地分类索引和 popup 快速启动体验。

## 基础原则

- 浏览器书签是主要网址来源，用户也可自行添加书签。
- 默认不移动、不删除、不重命名浏览器原始收藏夹内容。
- 插件只保存本地元数据，例如分类结构、书签分类关联、搜索设置、最近高频记录。
- 同一个书签可以属于多个分类路径，但不复制真实书签节点。
- 面向用户统一使用“分类”概念。
- 第一阶段只做 popup 体验。
- popup 前端不内置书签分类匹配规则。分类匹配、规则编辑、批量应用等能力归后台控制页负责。

## Popup 主结构

popup 建议宽度为 200-680px。

从上到下包含：

1. 顶部搜索，右侧预留后台设置小icon。
2. 三列级联分类区。
3. 固定在一级分类底部的“最近常用”入口。

## 顶部搜索

搜索框固定在顶部。

基础搜索匹配：

- 标题。
- URL。
- 域名。
- 分类。

搜索结果用于快速打开书签。

## 分类浏览

普通一级分类示例：

- 音乐
- 视频
- 工具
- 学习

分类规则：

- 分类是插件本地维护的树状索引，不等于浏览器收藏夹文件夹。
- 分类支持无限层级。
- 书签可以挂到任意分类节点。
- 一个书签可以同时挂到多个分类节点。
- 上级分类自动汇总所有下级分类中的书签。
- 同一书签可以在多个分类路径中出现。

展开某个分类时，内容顺序为：

1. 直接子分类。
2. 当前分类及所有下级分类汇总出来的书签。

子分类排序优先于书签排序。书签再按最近高频、用户置顶等规则排序。

## 最近常用

“最近常用”是特殊固定一级入口。

规则：

- 固定在普通一级分类底部。
- 用户滚动普通一级分类时保持可见。
- 不能创建子分类。
- 只展示最近高频网页。
- 第一阶段只统计插件内点击，后台设置部分只预留相应接口。

## 后台控制页规划

后台控制页用于管理偏配置、偏维护的能力，popup 只消费这些本地设置和本地数据。

后台管理开发说明、当前实现进度、存储字段、注意事项和后续任务已单独整理到 `BACKEND_CONTROL.md`。

后续开发后台管理页、后台同步、分类维护、书签归类、规则中心、AI 配置时，优先读取 `BACKEND_CONTROL.md`，不要把 `POPUP_INTERACTION.md` 当作后台开发的主要上下文。

核心边界仍保持：

- 分类匹配规则只在后台控制页维护，不写进 popup 前端代码。
- 批量应用只更新插件本地的书签分类关联，不修改浏览器原始收藏夹。
- 同步只读取浏览器书签树并保存本地快照，不移动、不删除、不重命名浏览器原始收藏夹内容。
- 默认不抓取网页正文，不请求外部 API，不硬编码 API Key 或密钥。
- 第一阶段不读取浏览器历史。

## 第一阶段范围

优先完成：

- popup 三列级联分类启动器。
- 分类节点树数据结构。
- 书签与分类节点的多关联关系。
- 分类向下汇总书签查询。
- 固定“最近常用”入口。
- 插件内点击统计。
- 本地基础搜索。

暂不做：

- 浏览器历史统计。
- 真实 AI 接口。
- 页面摘要抓取。
- popup 前端分类规则匹配。
- 自动整理真实浏览器收藏夹。
- 自动移动、删除、重命名浏览器原始书签。
